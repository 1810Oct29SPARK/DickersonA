function calculator (num1,num2,operation) /*using word function indicates start of a function declaration, calculator is the identifier * inside (arguments/parameters)*/
/* begins variable declaration */let result = 0;                                          /*value of the arguments is set when the function is called open with {}* if making argument in function use let or var puts in the scope of the function only instead of global can also comment with //(single line)*/
let add = number + number;
return result;
let sum = calculator (57,25,"add");

